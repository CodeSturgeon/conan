from setuptools import setup, find_packages

setup(
    name = 'conan',
    version = '0.1dev',
    description = 'file distribution with couch',
    author = 'Nick Fisher',
    packages = find_packages(),
    zip_safe = False
)
